rules_version = '2'

service cloud.firestore {
	match /databases/{database}/documents {
		match /users/{user} {
			allow read: if isSignedInWith(user)
		}

		function isSignedIn() {
			return request.auth != null
		}

		function isSignedInWith(id) {
			return isSignedIn() && request.auth.uid == id
		}
	}
}
